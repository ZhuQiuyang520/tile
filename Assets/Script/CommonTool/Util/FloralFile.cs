using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FloralFile
{
    public static string ZigzagMyBuy(double a)
    {
        return Math.Round(a, TemperLagoon.SnoutSender).ToString();
    }
    public static string ZigzagMyBuy(double a, int digits)
    {
        return Math.Round(a, digits).ToString();
    }

    public static double Snout(double a)
    {
        return Math.Round(a, TemperLagoon.SnoutSender);
    }

}
